import React from 'react';
import { Helmet } from 'react-helmet-async';

type Props = {};

const AppointmentManagement = (props: Props) => {
  return (
    <>
      <Helmet>
        <title>Job Seeker</title>
      </Helmet>
    </>
  );
};

export default AppointmentManagement;
